"""Django application configuration for {app_name_pretty}."""

from django.apps import AppConfig


class {app_name_config}(AppConfig):
    """Application configuration for {app_name_pretty}."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "{app_name}"
    verbose_name = "{app_name_pretty}"
